package week4_inheritance;

public interface Browser {
    void open();           // Bắt buộc phải có hàm mở
    void close();          // Bắt buộc phải có hàm đóng
    void takeScreenshot(); // Bắt buộc phải có hàm chụp ảnh
}
